<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CANCELACIÓN DE COTIZACIÓN</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            max-width: 600px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            font-size: 14px;
        }

        .banner {
            background-color: #0056b3;
            color: #ffffff;
            text-align: center;
            padding: 20px;
        }

        .content {
            padding: 20px;
        }

        h1 {
            margin-top: 0;
            font-size: 24px;
        }

        p {
            margin-bottom: 20px;
        }

        strong {
            font-weight: bold;
        }

        .button-container {
            text-align: center;
            margin: 20px 0;
        }

        .button {
            color: #ffffff;
            text-decoration: none;
            background-color: #0056b3;
            padding: 10px 20px;
            border-radius: 5px;
            font-weight: bold;
            display: inline-block;
        }

        .button:hover {
            background-color: #004080;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background-color: #f4f4f4;
        }

        .logo {
            display: block;
            margin: 0 auto;
            width: 50px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="banner">
            <h1>CONTRA RECIBO DE INGRESO CON FOLIO {{ response.id }}</h1>
        </div>
        <div class="content">
            <p>
                <strong>EVENTO:</strong> {{ response.nombre_evento }}<br>
                <strong>SEGMENTO DE NEGOCIO:</strong> {{ segmento_negocios }}<br>
                <strong>MONTO: $</strong> {{ response.ing_monto }}<br>
                <strong>FECHA DE PAGO:</strong> {{ response.fecha_pago }}<br>
                <strong>REGISTRADO POR:</strong> {{ response.usuario }}<br>
                <strong>CONCEPTO:</strong> {{ response.concepto }}<br>
                <strong>MODO DE PAGO:</strong> {{ response.modoPago }}<br>
                <strong>COMENTARIO DEL INGRESO:</strong><br>
                {{ response.ing_comentario }}
            </p>
        </div>
        <div class="footer">
            <img src="http://cms.mazatlanic.com/assets/images/logomic_correos.png" alt="Logo Mazatlanic" class="logo">
        </div>
    </div>
</body>
</html>
